name: release

on:
  workflow_dispatch:
  push:
    branches:
    - main
    - "track/**"

permissions:
    contents: write
    pull-requests: write

jobs:
  release:
      name: release
      uses: canonical/identity-team/.github/workflows/charm-release.yaml@e017941885bd1d17774bf7be2cbbb1d1abe80d9e # v1.16.0
      secrets:
        PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
