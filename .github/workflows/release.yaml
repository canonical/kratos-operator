name: release

on:
  workflow_dispatch:
  push:
    branches:
    - main
    - "track/**"

permissions:
    contents: write
    pull-requests: write

jobs:
  release:
      name: release
      uses: canonical/identity-team/.github/workflows/charm-release.yaml@6980fa34b31ca8631e6cb2d57980e7e035697a31 # v1.8.5
      secrets:
        PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
