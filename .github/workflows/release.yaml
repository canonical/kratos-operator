name: release

on:
  workflow_dispatch:
  push:
    branches:
    - main
    - "track/**"

permissions:
    contents: write
    pull-requests: write

jobs:
  release:
      name: release
      uses: canonical/identity-team/.github/workflows/charm-release.yaml@d64b860c8679d3539f5f35f3d2494c419d8e41b5 # v1.12.2
      secrets:
        PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
